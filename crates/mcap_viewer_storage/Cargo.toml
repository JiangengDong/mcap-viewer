[package]
name = "mcap_viewer_storage"
version = "0.1.0"
edition = "2021"

[dependencies]
delegate = "0.10.0"
egui_plot = { version = "0.24.1", optional = true }
hashbrown = "0.14.1"
mcap_decoder = { version = "0.1.0", path = "../mcap_decoder" }

[features]
default = ["egui_plot"]
